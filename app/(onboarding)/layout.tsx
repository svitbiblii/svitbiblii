const OnboardingLayout = ({
    children
}: {
    children: React.ReactNode;
}) => {
    return (
        <div className="">
            {children}
        </div>
    )
}
export default OnboardingLayout;