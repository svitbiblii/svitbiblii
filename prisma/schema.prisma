generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider     = "postgresql"
  url          = env("DATABASE_URL")
  relationMode = "prisma"
}

model books {
  id      Int     @id @default(autoincrement())
  name    String?
  content String?
  title   String?
  author  String?
}


